# Recall Touch Voice Server v2.0

# Core server
fastapi==0.115.0
uvicorn==0.30.0
websockets==12.0
pydantic>=2.0
httpx>=0.27.0
requests>=2.31.0
python-multipart>=0.0.6

# Audio processing
numpy==1.26.4
soundfile==0.12.1
scipy>=1.11.0

# ML / TTS / STT
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0

# Orpheus TTS (primary — best quality, emotion support)
# SNAC codec for Orpheus token-to-audio conversion
snac>=0.2.0

# Fish Speech (multilingual fallback)
# CPU free-tier deploys skip this package because compatible wheels are not
# available in the Fly remote builder environment.
# fishspeech>=1.5.0

# STT — Faster-Whisper (4x faster than OpenAI Whisper)
faster-whisper==1.0.3
ctranslate2>=4.4.0

# Edge TTS — FREE Microsoft Neural voices (no API key, no cost)
edge-tts>=6.1.0
pydub>=0.25.1

# Optional: vLLM for maximum TTS throughput (16+ concurrent streams)
# pip install vllm>=0.4.0
