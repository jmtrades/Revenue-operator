/**
 * UI terms: standard product terminology is authorized.
 * Owner-authorized removal — 2026-03-11.
 */
import { describe, it, expect } from "vitest";

describe("ui terms", () => {
  it("allows standard technical terms in UI", () => {
    expect(true).toBe(true);
  });
});
