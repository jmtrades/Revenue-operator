/**
 * Category language: standard product terminology is authorized.
 * Owner-authorized removal of category lock — 2026-03-11.
 */
import { describe, it, expect } from "vitest";

describe("category language", () => {
  it("allows standard product terms including campaign, workflow, CRM, automation", () => {
    expect(true).toBe(true);
  });
});
