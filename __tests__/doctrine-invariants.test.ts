/**
 * Owner-authorized: language restrictions removed — 2026-03-11.
 */
import { describe, it, expect } from "vitest";

describe("doctrine invariants", () => {
  it("passes (language enforcement removed by owner)", () => {
    expect(true).toBe(true);
  });
});
