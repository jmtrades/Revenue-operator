# RECALL TOUCH — THE LAST CURSOR PROMPT
# Paste as .cursorrules. Final pass. Read ALL. Fix ALL. Polish ALL. Then deploy.

CONTEXT: Recall Touch — AI phone system for businesses. Next.js + Tailwind. Premium product feel.

REPO RECON: npm run build (fix all errors). npm run lint (fix all warnings). Map routes. Grep TODO/FIXME.

VERIFIED BUGS TO FIX:
1. Sign-in: Real email+password form. No duplicate "Welcome back". If email matches localStorage('rt_signup') → set rt_authenticated → redirect /app. Else "No account found. Get started free →". Google → toast "Coming soon". Forgot password → toast "Check your email". Navbar on sign-in page.
2. Demo: Auto-play ALL messages. Typing indicator (3 dots) 0.8s before each AI message. Timer counts up from 0:00. Result card after last message. Skip to result.
3. Buttons: Primary CTA = bg-white text-black EVERYWHERE (not blue/indigo). .btn-marketing-primary = white bg. Kill blue/indigo buttons.
4. Homepage preview: 3–5 call cards animate in (slide-up). No empty area.
5. Pricing toggle: Monthly $97/$247/$497, Annual $81/$206/$414, "Save 17%".
6. Onboarding: Minimal sidebar during steps (or hide). Welcome message. Helper text. Continue button white/black. Step 5 confetti.
7. Social proof: Testimonials + "Trusted by 200+ businesses". Industry badges.
8. Empty space above waitlist: Tighten or add comparison/social proof.
9. Verify all pages: /product, /pricing, /docs, /contact, /privacy, /terms, /blog, industries, /app/*, error, not-found, sitemap, robots.
10. Anti-churn: Checklist, milestone cards, inactivity banner, ROI card, cancel flow, toasts.
11. Polish: Mobile 375px, no overflow, skeletons, focus rings, a11y.

DESIGN: bg-black, bg-zinc-950 sidebar, bg-zinc-900/50 cards, text-white headings, text-zinc-400 body. Primary btn: bg-white text-black rounded-xl font-semibold. Inputs: bg-zinc-900 border-zinc-800 rounded-xl.

ZERO TOLERANCE: No "Verifying session". No empty pages. No dead buttons. No duplicate headings. No "coming soon" on shipped pages. No blue/indigo primary buttons. build + lint pass.
