/**
 * Coordination displacement: expected human clarification did not occur; action used the environment.
 * Deterministic: state + timing only.
 */

export type ActorType = "staff" | "customer" | "counterparty";
export type DecisionType = "attendance" | "payment" | "responsibility" | "confirmation" | "continuation";
