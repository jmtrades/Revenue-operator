"use client";

export function ConfidenceContractBanner() {
  return (
    <div className="px-4 py-2 bg-sky-950/30 border-b border-sky-800/50 flex items-center gap-2 text-sm">
      <span className="text-sky-400 shrink-0">Accountability:</span>
      <span className="text-sky-200">
        If protection fails, you&apos;ll see it immediately. Misses and violations surface in real time.
      </span>
    </div>
  );
}
